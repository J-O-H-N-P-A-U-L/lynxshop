---
title: Boilerplate web store: proudly powered by Contentful, Middleman & Snipcart
---

<div class="row">
  <h1>
    Products
  </h1>
</div>

<div class="row">
    <% data.contentful.product.each do |elem| %>
        <% p = elem[1] %>
        <div class="small-6 columns">
          <a href='<%= "products/#{p.slug}.html" %>'>
            <% p.image.each do |image| -%>
                <%= image_tag(image.url, :alt => image.title, :class => "thumbnail") %>
            <% end -%>
          </a>

          <div>
            <strong><%= p.productName %></strong>
          </div>

          <div>
            <%= number_to_currency(p.price, :unit => "$") %>
          </div>

        </div>
    <% end %>
</div>

